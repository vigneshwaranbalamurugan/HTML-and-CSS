# Tabbed Content Interface

# Objective

- Create a tabbed interface where clicking on a tab shows different content sections.

# Features

- Utilized radio along with labels to serve as tabs.
- Used the :checked pseudo-class to display the corresponding content while hiding others.
- Incorporated the smooth transitions when switching between tabs.